(function(){
const STORAGE_KEY='vehicle_document_expiry_app_supabase_v1';
const TARGET_DOCS=['Insurance','PUC','Fitness','Tax'];
const TESSERACT_URL='https://cdn.jsdelivr.net/npm/tesseract.js@5/dist/tesseract.min.js';
function readVehicles(){try{const data=JSON.parse(localStorage.getItem(STORAGE_KEY)||'[]');return Array.isArray(data)?data:[]}catch{return[]}}
function saveVehicles(v){localStorage.setItem(STORAGE_KEY,JSON.stringify(v))}
function normalizeText(v){return String(v||'').toLowerCase().replace(/0/g,'o').replace(/[^a-z0-9\s]/g,' ').replace(/\s+/g,' ').trim()}
function cleanVehicleNo(v){return String(v||'').toUpperCase().replace(/[^A-Z0-9]/g,'')}
function extractVehicleNo(text){const compact=String(text||'').toUpperCase().replace(/[^A-Z0-9]/g,'');const m=compact.match(/[A-Z]{2}\d{1,2}[A-Z]{1,3}\d{3,5}/);return m?cleanVehicleNo(m[0]):''}
function autofillVehicleInput(no){if(!no)return;const input=[...document.querySelectorAll('input')].find(i=>/vehicle\s*(no|number)|vehicleno/i.test((i.placeholder||'')+' '+(i.name||'')+' '+(i.id||'')));if(!input)return;input.value=no;input.dispatchEvent(new Event('input',{bubbles:true}));input.dispatchEvent(new Event('change',{bubbles:true}))}
function normalizeDate(raw){if(!raw)return'';const clean=String(raw).replace(/[,]/g,' ').replace(/\s+/g,' ').trim();const months={jan:1,january:1,feb:2,february:2,mar:3,march:3,apr:4,april:4,may:5,jun:6,june:6,jul:7,july:7,aug:8,august:8,sep:9,sept:9,september:9,oct:10,october:10,nov:11,november:11,dec:12,december:12};const numeric=clean.match(/(\d{1,2})[\/\-.\s](\d{1,2})[\/\-.\s](\d{2,4})/);if(numeric){let day=Number(numeric[1]),month=Number(numeric[2]),year=Number(numeric[3]);if(year<100)year+=2000;if(month>12&&day<=12){const t=day;day=month;month=t}return[year,String(month).padStart(2,'0'),String(day).padStart(2,'0')].join('-')}const dmy=clean.match(/(\d{1,2})\s*[-\/.]?\s*([A-Za-z]{3,9})\s*[-\/.]?\s*(\d{2,4})/i);if(dmy){const day=Number(dmy[1]);const month=months[dmy[2].toLowerCase()];let year=Number(dmy[3]);if(year<100)year+=2000;if(month)return[year,String(month).padStart(2,'0'),String(day).padStart(2,'0')].join('-')}return''}
function getDatePattern(){return/(\d{1,2}[\/\-.\s]\d{1,2}[\/\-.\s]\d{2,4}|\d{1,2}\s*[-\/.]?\s*[A-Za-z]{3,9}\s*[-\/.]?\s*\d{2,4})/i}
function pickDateNearKeyword(text,keywords){const lines=String(text||'').split(/\n+/).map(l=>l.trim()).filter(Boolean);const datePattern=getDatePattern();for(let i=0;i<lines.length;i++){const lower=normalizeText(lines[i]);if(keywords.some(kw=>lower.includes(kw))){const nearby=[lines[i],lines[i+1]||'',lines[i+2]||'',lines[i+3]||''].join(' ');const match=nearby.match(datePattern);const normalized=normalizeDate(match&&match[1]);if(normalized)return normalized}}return''}
function extractDates(text){return{VehicleNo:extractVehicleNo(text),Insurance:pickDateNearKeyword(text,['insurance','insur','policy']),PUC:pickDateNearKeyword(text,['pucc','puc','pollution']),Fitness:pickDateNearKeyword(text,['fitness','fit upto']),Tax:pickDateNearKeyword(text,['tax valid','road tax'])}}
function loadTesseract(){return new Promise((resolve,reject)=>{if(window.Tesseract)return resolve(window.Tesseract);const s=document.createElement('script');s.src=TESSERACT_URL;s.onload=()=>window.Tesseract?resolve(window.Tesseract):reject(new Error('OCR library load failed'));s.onerror=()=>reject(new Error('OCR library download failed'));document.head.appendChild(s)})}
function ensureDoc(vehicle,docName,expiryDate){const docs=Array.isArray(vehicle.docs)?[...vehicle.docs]:[];const index=docs.findIndex(item=>String(item.name||'').toLowerCase()===docName.toLowerCase());if(index>=0)docs[index]={...docs[index],expiryDate};else docs.push({name:docName,expiryDate});return{...vehicle,docs,updatedAt:new Date().toISOString()}}
function applyExtractedData(vehicleId,extracted){const next=readVehicles().map(vehicle=>{if(String(vehicle.id)!==String(vehicleId))return vehicle;let updated={...vehicle};if(extracted.VehicleNo)updated.vehicleNo=cleanVehicleNo(extracted.VehicleNo);TARGET_DOCS.forEach(docName=>{if(extracted[docName])updated=ensureDoc(updated,docName,extracted[docName])});return{...updated,updatedAt:new Date().toISOString()}});saveVehicles(next)}
function renderPreview(container,extracted){const vehicleRow='<label style="display:block;margin:8px 0;font-size:12px;font-weight:700">Vehicle No<input data-vehicle-no="1" type="text" value="'+(extracted.VehicleNo||'')+'" placeholder="Vehicle No not detected" style="width:100%;margin-top:4px;padding:8px;border:1px solid #cbd5e1;border-radius:10px;text-transform:uppercase" /></label>';const rows=TARGET_DOCS.map(docName=>'<label style="display:block;margin:8px 0;font-size:12px;font-weight:700">'+docName+'<input data-doc="'+docName+'" type="date" value="'+(extracted[docName]||'')+'" style="width:100%;margin-top:4px;padding:8px;border:1px solid #cbd5e1;border-radius:10px" /></label>').join('');container.querySelector('.ocrPreview').innerHTML=vehicleRow+rows}
function selectVehicleByNo(select,no){const clean=cleanVehicleNo(no);if(!select||!clean)return;const match=readVehicles().find(v=>cleanVehicleNo(v.vehicleNo)===clean);if(match)select.value=String(match.id)}
function addPanel(){if(document.getElementById('screenshotImportPanel'))return;const vehiclesTop=document.querySelector('.vehiclesTop');if(!vehiclesTop)return;const panel=document.createElement('section');panel.id='screenshotImportPanel';panel.className='card';panel.style.marginBottom='10px';panel.innerHTML='<div class="sectionHead"><h2>Import Screenshot</h2></div><p style="font-size:12px;color:#64748b;margin:0 0 8px">Upload vehicle document screenshot to automatically detect Vehicle No, Insurance, PUC, Fitness and Tax dates.</p><select class="ocrVehicle" style="width:100%;padding:10px;border:1px solid #cbd5e1;border-radius:12px;margin-bottom:8px;color:#111827;background:#fff;min-height:48px"></select><input class="ocrFile" type="file" accept="image/*" style="width:100%;padding:10px;border:1px solid #cbd5e1;border-radius:12px;margin-bottom:8px" /><button class="ocrRead" style="background:#2563eb;color:white;border:0;border-radius:10px;padding:10px 14px;font-weight:700">Read Screenshot</button><div class="ocrStatus" style="font-size:12px;color:#64748b;margin-top:8px">No screenshot selected.</div><div class="ocrPreview" style="margin-top:8px"></div><button class="ocrApply" style="display:none;background:#16a34a;color:white;border:0;border-radius:10px;padding:10px 14px;font-weight:700;margin-top:8px">Confirm & Save Dates</button>';vehiclesTop.insertAdjacentElement('beforebegin',panel);const vehicleSelect=panel.querySelector('.ocrVehicle'),status=panel.querySelector('.ocrStatus'),fileInput=panel.querySelector('.ocrFile'),applyButton=panel.querySelector('.ocrApply');let extracted={};function refreshVehicles(){const vehicles=readVehicles().filter(v=>v&&v.id);const current=vehicleSelect.value;if(!vehicles.length){vehicleSelect.innerHTML='<option value="">Vehicles loading... please wait</option>';return false}vehicleSelect.innerHTML=vehicles.map(v=>'<option value="'+String(v.id).replace(/"/g,'&quot;')+'">'+(v.vehicleNo||'Vehicle')+' - '+(v.model||v.category||'Model')+'</option>').join('');const values=vehicles.map(v=>String(v.id));if(current&&values.includes(String(current)))vehicleSelect.value=current;else vehicleSelect.selectedIndex=0;return true}refreshVehicles();const refreshTimer=setInterval(refreshVehicles,1000);setTimeout(()=>clearInterval(refreshTimer),20000);['focus','click','touchstart','change'].forEach(e=>vehicleSelect.addEventListener(e,refreshVehicles));document.addEventListener('visibilitychange',refreshVehicles);window.addEventListener('storage',refreshVehicles);panel.querySelector('.ocrRead').onclick=async()=>{refreshVehicles();const file=fileInput.files&&fileInput.files[0];if(!file)return alert('Pehle screenshot select karo.');try{status.textContent='OCR library loading...';const Tesseract=await loadTesseract();status.textContent='Reading screenshot...';const result=await Tesseract.recognize(file,'eng',{logger:m=>{if(m&&m.status)status.textContent='OCR: '+m.status+(m.progress?' '+Math.round(m.progress*100)+'%':'')}});const text=result&&result.data?result.data.text:'';extracted=extractDates(text);if(extracted.VehicleNo){autofillVehicleInput(extracted.VehicleNo);selectVehicleByNo(vehicleSelect,extracted.VehicleNo)}renderPreview(panel,extracted);applyButton.style.display='inline-block';const foundCount=TARGET_DOCS.filter(docName=>extracted[docName]).length;status.textContent=foundCount+' date found. '+(extracted.VehicleNo?'Vehicle No: '+extracted.VehicleNo+'.':'Vehicle No not detected.')+' Preview check karo.'}catch(error){status.textContent='OCR failed: '+(error.message||'unknown error')}};applyButton.onclick=()=>{refreshVehicles();const vehicleId=vehicleSelect.value;if(!vehicleId)return alert('Vehicle select nahi hai. Page reload karke dobara try karo.');const edited={};const vInput=panel.querySelector('.ocrPreview input[data-vehicle-no]');if(vInput)edited.VehicleNo=cleanVehicleNo(vInput.value);panel.querySelectorAll('.ocrPreview input[data-doc]').forEach(input=>{edited[input.dataset.doc]=input.value});applyExtractedData(vehicleId,edited);alert('Vehicle Data Saved Successfully ✅');location.reload()}}
const timer=setInterval(addPanel,800);setTimeout(()=>{clearInterval(timer);addPanel()},12000);
})();
